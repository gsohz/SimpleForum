{{#each errorsU}}
<div class="alert alert-danger">
  {{text}}
</div>

{{else}}

{{/each}}

<h3>Entrar</h3>
<div class="card d-flex align-item-center">
  <div class="card-body">
    <form action="/login" method="POST">
      <label for="email">Email: </label>
      <input type="email" name="email" class="form-control" required>
      <label for="password">Senha: </label>
      <input type="password" name="password" class="form-control" required>
      <button type="submit" class="btn btn-success mt-4">Login</button>
    </form>
<br>
    <small>Ainda não possui uma conta? Crie uma para ter acesso total ao site <a class="btn btn-primary btn-sm" href="/registro" role="button">Crie uma conta</a></small>
  </div>
</div>