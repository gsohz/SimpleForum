{{!-- Show message for each error --}}
{{#each errorsC}}
<div class="alert alert-danger">
  {{text}}
</div>

{{else}}

{{/each}}
<h2>Nova categoria</h2>
{{!-- Add new category --}}
<div class="card">
  <div class="card-body">

      {{!-- Form new category --}}
      <form action="/categoria/add" method="POST">
        <label for="name">Categoria: </label>
        <input type="text" name="name" placeholder="Digite a categoria..." class="form-control">
        <label for="tag">tag: </label>
        <input name="tag" placeholder="Digite o tag..." class="form-control">
        <br>
        <hr>
        <button type="submit" class="btn btn-success">Enviar</button>
      </form>

  </div>
</div>